diff --git a/CMakeLists.txt b/CMakeLists.txt
index f91023a..68c0686 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1,5 +1,7 @@
 ﻿cmake_minimum_required (VERSION 3.15)
 
+include(FindPkgConfig)
+
 set(CMAKE_CXX_STANDARD 20)
 
 cmake_policy(SET CMP0091 NEW) # for MSVC_RUNTIME_LIBRARY
@@ -29,8 +31,8 @@ include_directories(PRIVATE
 ${CMAKE_CURRENT_SOURCE_DIR}/include
 )
 
-find_package(zstd REQUIRED)
-target_link_libraries(zarchive PRIVATE zstd::libzstd_static)
+pkg_check_modules(PC_ZSTD libzstd REQUIRED)
+target_link_libraries(zarchive PRIVATE ${PC_ZSTD_LIBRARIES})
 
 # standalone executable
 add_executable (zarchiveTool src/main.cpp)
