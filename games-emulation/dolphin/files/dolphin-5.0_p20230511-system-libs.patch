diff --git a/CMakeLists.txt b/CMakeLists.txt
index 06ddaf127f..5a946c572d 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -770,7 +770,10 @@ else()
   add_subdirectory(Externals/enet)
 endif()
 
-if(NOT XXHASH_FOUND)
+pkg_check_modules(XXHASH libxxhash IMPORTED_TARGET)
+if (XXHASH_FOUND)
+	add_library(xxhash ALIAS PkgConfig::XXHASH)
+else()
   message(STATUS "Using static xxhash from Externals")
   add_subdirectory(Externals/xxhash)
 endif()
@@ -806,15 +809,25 @@ else()
   add_subdirectory(Externals/zstd)
 endif()
 
-add_subdirectory(Externals/zlib-ng)
+if (NOT FORCE_BUNDLED_ZLIB_NG)
+	pkg_check_modules(ZLIB_NG QUIET zlib-ng IMPORTED_TARGET)
+endif()
+if (ZLIB_NG_FOUND)
+  message(STATUS "Using shared zlib-ng version: " ${ZLIB_NG_VERSION})
+  add_library(ZLIB::ZLIB ALIAS PkgConfig::ZLIB_NG)
+else()
+  add_subdirectory(Externals/zlib-ng)
+endif()
 
-pkg_check_modules(MINIZIP minizip>=3.0.0)
+if (NOT FORCE_BUNDLED_MINIZIP)
+  pkg_check_modules(MINIZIP minizip>=3.0.0)
+endif()
 if(MINIZIP_FOUND)
   message(STATUS "Using shared minizip")
   include_directories(${MINIZIP_INCLUDE_DIRS})
 else()
   check_vendoring_approved(minizip)
-  message(STATUS "Shared minizip not found, falling back to the static library")
+  message(STATUS "Using bundled minizip-ng static library")
   add_subdirectory(Externals/minizip)
   include_directories(External/minizip)
 endif()
