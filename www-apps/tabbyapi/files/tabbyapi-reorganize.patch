--- a/pyproject.toml
+++ b/pyproject.toml
@@ -7,8 +7,7 @@ requires = [
 build-backend = "setuptools.build_meta"
 
-# We're not building the project itself
 [tool.setuptools]
-py-modules = []
+packages = ["tabbyapi"]
 
 [project]
